<com.ch.co.ui.RevealLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="12dp"
    tools:context="${relativePackage}.${activityClass}" >

    <Button
        android:id="@+id/viewBasic"
        style="@style/AppTheme.Button.Green"
        android:onClick="onButtonClick"
        android:text="View基础" />

    <Button
        android:id="@+id/scroll_view_conflict_extern_interception"
        style="@style/AppTheme.Button.Green"
        android:onClick="onButtonClick"
        android:text="滑动冲突场景1-外部拦截" />

    <Button
        android:id="@+id/scroll_view_conflict_internal_interception"
        style="@style/AppTheme.Button.Green"
        android:onClick="onButtonClick"
        android:text="滑动冲突场景1-内部拦截" />

</com.ch.co.ui.RevealLayout>